# Program to find the length of a list using the len() function

# Sample list
sample_list = [10, 20, 30, 40, 50]

# Finding the length of the list
list_length = len(sample_list)

# Display the result
print("The length of the list is:", list_length)